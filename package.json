{
  "name": "md-preview",
  "version": "0.1.0",
  "description": "Lightweight cross-platform Markdown preview app (Tauri v2)",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
